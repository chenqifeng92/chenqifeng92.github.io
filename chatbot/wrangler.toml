name = "qifeng-chatbot"
main = "worker.js"
compatibility_date = "2025-01-01"

# 部署后用 wrangler secret put DEEPSEEK_API_KEY 设置密钥，不要写在这里。
# 本地开发用 .dev.vars 文件（已在 .gitignore），格式：
#   DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
